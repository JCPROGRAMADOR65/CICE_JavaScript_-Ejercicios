/*3.  Escribe una función que devuelva el resultado de sumar los dos parámetros que le
pasamos. Muestra el resultado en la consola.
*/

console.log(sumaFuncion(3,4))

function sumaFuncion(Arg1, Arg2) {
  let suma = Arg1 = Arg2
  return suma
}

// lo sigiente hace lo mismo//
console.log(sumaFuncion(3,4))

function sumaFuncion(Arg1, Arg2) {
  return   Arg1 + Arg2
}